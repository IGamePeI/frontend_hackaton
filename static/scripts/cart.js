container
let cart = localStorage.getItem("cart");
if(!cart) {
    cart = [];
}

container = document.getElementById();

function createDishCard(dish) {
}

document.addEventListener('DOMContentLoaded', async() => {
    cart.forEach(dish_element => {
        createDishCard(dish_element);
    });
});